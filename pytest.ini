[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=. --ignore=utils/common.py
markers =
    slow: marks tests as slow
    integration: marks tests as integration tests
